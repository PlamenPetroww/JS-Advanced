function string(x, y, z) {

    let textL = x.length + y.length + z.length;
    let avg = Math.floor(textL / 3);
    console.log(textL)
    console.log(avg)

}

string('chocolate', 'ice cream', 'cake')